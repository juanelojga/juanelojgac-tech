#!/usr/bin/env sh
. "$(dirname -- "$0")/_/husky.sh"

# Pre-push tests disabled per user request
# Tests will run in CI/CD pipeline on GitHub Actions

# Uncomment below to re-enable pre-push testing:
# echo "🧪 Running Playwright tests before push..."
# echo "This may take a few minutes. To skip, use: git push --no-verify"
# echo ""
#
# # Run tests on chromium only for speed
# # Full browser matrix will run in CI/CD
# npm run test:e2e:chromium
#
# if [ $? -ne 0 ]; then
#   echo ""
#   echo "❌ Playwright tests failed!"
#   echo "Fix the failing tests before pushing, or use --no-verify to skip (not recommended)"
#   exit 1
# fi
#
# echo ""
# echo "✅ All tests passed! Proceeding with push..."

exit 0
